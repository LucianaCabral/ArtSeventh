dependencies {
    // Tests
    testImplementation(TestLibs.jUnitVersion)
    testImplementation(TestLibs.coroutineTest)
    testImplementation(TestLibs.turbine)
    testImplementation(TestLibs.koinJunit4)
    testImplementation(TestLibs.coreTesting)
    testImplementation(TestLibs.mockk)
    testImplementation(TestLibs.kotlinTest)
    testImplementation(TestLibs.koinTest)
    testImplementation(TestLibs.archCoreTest)
    testImplementation(TestLibs.mockServerTest)

    //Test android
    androidTestImplementation(TestLibs.jUnitVersion)
    androidTestImplementation(TestLibs.extJunit)
    androidTestImplementation(TestLibs.espressoCore)
}